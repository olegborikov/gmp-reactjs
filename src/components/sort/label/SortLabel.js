import React from 'react';
import classes from "./SortLabel.module.css";

function SortLabel() {
  return (
    <div className={classes.label}>
      SORT BY
    </div>
  );
}

export default SortLabel;
